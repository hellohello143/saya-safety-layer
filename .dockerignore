# Never bake secrets or local state into the image.
.env
.env.*
!.env.example
data/
*.db
*.db-journal
*.sqlite

# Rebuilt inside the image / not needed.
node_modules/
dist/

# VCS / editor / logs
.git/
.gitignore
*.log
.DS_Store
Thumbs.db
.claude/
